package ch.hslu.oop.sw12.temperature;

/**
 * The {@code EventType} enum represents the type of temperature event that can occur
 * within a {@code TemperatureCurve}. It is used to distinguish between events related
 * to maximum and minimum temperature changes.
 */
public enum EventType {
    MAX,
    MIN
}
